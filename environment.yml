# generated by meg33
# - complete environment.yml with Conda + Pip Integration
# - removed Build-Hashes 
# - to run in terminal, use:
# conda env create -f environment.yml
# conda activate myenv


# environment.yml
name: myenv
channels:
  - defaults
  - conda-forge
dependencies:
  # System/Conda Packages
  - python=3.12.8
  #  - python=3.12  # if you want remove version lock (instead 3.12.8)
  - bzip2=1.0.8
  - ca-certificates=2024.12.31
  - expat=2.6.4
  - libcxx=14.0.6
  - libffi=3.4.4
  - ncurses=6.4
  - openssl=3.0.15
  - readline=8.2
  - sqlite=3.45.3
  - tk=8.6.14
  - tzdata=2025a
  - xz=5.4.6
  - zlib=1.2.13
  
  # Python Base Packages
  - pip=25.0
  - setuptools=75.8.0
  - wheel=0.45.1

  # PyPI Packages (via pip)
  - pip:
    - numpy==1.26.4  # Original was 2.2.2 (doesnt exist on PyPI!)
    - pydub==0.25.1
    - soundfile==0.13.1
    - cffi==1.17.1
    - pycparser==2.22
