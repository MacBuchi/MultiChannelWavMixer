# Dependencies are managed by UV via pyproject.toml.
# Use the following commands to set up the environment:
#
#   uv sync          – create .venv and install all dependencies
#   uv run MultiChannelWavMixer.py   – run the application
#
# Legacy pip-installable list (kept for reference):
customtkinter>=5.2.2
librosa>=0.10.0
matplotlib>=3.10.0
numpy>=1.26.4
pyloudnorm>=0.1.0
pydub>=0.25.1
soundfile>=0.13.1